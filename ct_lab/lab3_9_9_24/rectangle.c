#include<stdio.h>
int main()
{
float l,b,area;
printf("enter length and bredth of a rectangle");
scanf("%f%f",&l,&b);
area=l*b;
printf("length=%f\nbredth=%f\n",l,b);
printf("area of rectangle=%f",area);
}
